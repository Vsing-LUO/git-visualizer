<Window x:Class="GitVisualizer.App.Dialogs.CredentialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTPS 凭据" Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="当前仓库" VerticalAlignment="Center"/>
        <TextBox x:Name="RepositoryAddressBox"
                 Grid.Column="1"
                 Margin="0,4"
                 Padding="7,5"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"/>
        <TextBlock Grid.Row="1" Text="用户名" VerticalAlignment="Center"/>
        <TextBox x:Name="UserNameBox" Grid.Row="1" Grid.Column="1" Margin="0,4"/>
        <TextBlock Grid.Row="2" Text="访问令牌" VerticalAlignment="Center"/>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,4">
            <PasswordBox x:Name="TokenBox"
                         Padding="7,5"
                         PasswordChanged="TokenBox_OnPasswordChanged"/>
            <TextBox x:Name="VisibleTokenBox"
                     Padding="7,5"
                     Visibility="Collapsed"
                     TextChanged="VisibleTokenBox_OnTextChanged"/>
        </Grid>
        <CheckBox x:Name="ShowTokenBox"
                  Grid.Row="3" Grid.Column="1"
                  Margin="0,6,0,2"
                  Content="显示访问令牌"
                  Checked="ShowTokenBox_OnChecked"
                  Unchecked="ShowTokenBox_OnUnchecked"/>
        <TextBlock x:Name="CredentialStatusText"
                   Grid.Row="4" Grid.Column="1"
                   Margin="0,4,0,2"
                   FontSize="11"
                   Opacity="0.65"/>
        <CheckBox x:Name="RememberBox" Grid.Row="5" Grid.Column="1" Margin="0,8"
                  Content="保存到 Windows 凭据管理器" IsChecked="True"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="DeleteCredentialButton"
                    Content="删除当前仓库凭据"
                    Foreground="#C62828"
                    Margin="0,0,12,0"
                    Click="DeleteCredential_OnClick"/>
            <Button Content="取消" IsCancel="True"/>
            <Button Content="保存" IsDefault="True" Click="Save_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
