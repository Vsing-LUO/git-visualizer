<Window x:Class="GitVisualizer.App.Dialogs.CredentialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HTTPS 凭据" Width="470" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="用户名" VerticalAlignment="Center"/>
        <TextBox x:Name="UserNameBox" Grid.Column="1" Margin="0,4"/>
        <TextBlock Grid.Row="1" Text="访问令牌" VerticalAlignment="Center"/>
        <PasswordBox x:Name="TokenBox" Grid.Row="1" Grid.Column="1" Margin="0,4" Padding="7,5"/>
        <CheckBox x:Name="RememberBox" Grid.Row="2" Grid.Column="1" Margin="0,8"
                  Content="保存到 Windows 凭据管理器" IsChecked="True"/>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" IsCancel="True"/>
            <Button Content="保存" IsDefault="True" Click="Save_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
