<Window x:Class="GitVisualizer.App.Dialogs.ThemedMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520"
        MaxWidth="680"
        MaxHeight="760"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="Foreground" Value="#F2F5FA"/>
            <Setter Property="Background" Value="#162233"/>
            <Setter Property="BorderBrush" Value="#40536D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#1C2C42"/>
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#64748B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#203552"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryDialogButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#1E6BFF"/>
            <Setter Property="BorderBrush" Value="#60A5FA"/>
        </Style>
        <Style x:Key="DangerDialogButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#B4232D"/>
            <Setter Property="BorderBrush" Value="#EF646B"/>
        </Style>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#26364B"/>
                                <Setter Property="Foreground" Value="#F2F5FA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#0D1623" BorderBrush="#3D4F6A" BorderThickness="1" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="26" ShadowDepth="8" Opacity="0.48" Color="#02060D"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Background="#090F19" BorderBrush="#26364B" BorderThickness="0,0,0,1"
                    CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                <Grid Margin="14,0,6,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid Width="24" Height="24" VerticalAlignment="Center">
                        <Ellipse Fill="#142E59" Stroke="#3B82F6" StrokeThickness="1"/>
                        <Ellipse Width="5" Height="5" Fill="#7DD3FC"/>
                        <Path Data="M 4,15 C 8,15 8,9 12,9 C 16,9 16,15 20,15" Stroke="#3B82F6"
                              StrokeThickness="1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                    </Grid>
                    <TextBlock x:Name="CaptionText" Grid.Column="1" Margin="10,0,8,0"
                               VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"
                               Foreground="#F2F5FA" TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="2" Style="{StaticResource TitleBarButton}" Content="✕" FontSize="14"
                            ToolTip="关闭" Click="Close_OnClick"/>
                </Grid>
            </Border>
            <Grid Grid.Row="1" Margin="24,24,28,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="MessageIconBorder" Width="46" Height="46" HorizontalAlignment="Left"
                        VerticalAlignment="Top" Background="#241E6BFF" BorderBrush="#663B82F6"
                        BorderThickness="1" CornerRadius="23">
                    <TextBlock x:Name="MessageIconText" HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontFamily="Segoe UI" FontSize="23" FontWeight="SemiBold" Foreground="#60A5FA"/>
                </Border>
                <ScrollViewer Grid.Column="1" MaxHeight="390" VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="MessageText" MinWidth="330" MaxWidth="540" VerticalAlignment="Center"
                               FontFamily="Microsoft YaHei UI, Segoe UI" FontSize="14" LineHeight="23"
                               Foreground="#E7EDF7" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
            <Border Grid.Row="2" Padding="20,14" Background="#0A111C" BorderBrush="#26364B"
                    BorderThickness="0,1,0,0" CornerRadius="0,0,10,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="TertiaryActionButton" Style="{StaticResource DialogButton}"
                            Visibility="Collapsed" Click="ActionButton_OnClick"/>
                    <Button x:Name="SecondaryActionButton" Style="{StaticResource DialogButton}"
                            Visibility="Collapsed" Click="ActionButton_OnClick"/>
                    <Button x:Name="PrimaryActionButton" Style="{StaticResource PrimaryDialogButton}"
                            IsDefault="True" Click="ActionButton_OnClick"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
