<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="GitVisualizer.App.Dialogs.EditorSafetyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑器安全确认" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Name="TitleText" FontSize="20" FontWeight="SemiBold" />
    <TextBlock Name="MessageText" Grid.Row="1" Margin="0,12,0,20" TextWrapping="Wrap" Opacity="0.78" />
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="取消" IsCancel="True" />
      <Button Name="SecondaryButton" Margin="8,0,0,0" Click="Secondary_OnClick" />
      <Button Name="PrimaryButton" Margin="8,0,0,0" IsDefault="True" Click="Primary_OnClick" />
    </StackPanel>
  </Grid>
</Window>
