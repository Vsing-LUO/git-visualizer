# Git 可视化项目交接文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | Git 可视化 |
| 项目类型 | Windows 桌面 Git 客户端 |
| 当前阶段 | 可运行的 V1 原型，适合继续开发和功能验证，尚不建议作为生产级 Git 客户端发布 |
| 交接日期 | 2026-07-26 |
| 功能基线 | `main` 分支，功能提交 `7ab0e7c` |
| 源码位置 | `C:\Users\ZHAO\OneDrive\Desktop\git可视化` |
| 远程仓库 | 未配置；当前仅使用本地 Git 仓库 |

## 2. 项目概况与作用

本项目是一个面向 Git 初学者和普通开发者的中文 Windows 桌面客户端。它的目标是把常用 Git 指令转换为可视化操作，让用户通过仓库导航、文件树、拖放、按钮、提交关系图和差异视图完成日常版本管理，尽量不要求用户安装、记忆或输入 Git CLI 命令。

项目不提供 GitHub、GitLab、Gitee 等平台的账号、PR、Issue 或代码评审功能，也不包含 Git 服务端。远程操作面向普通 Git 仓库地址。

当前版本的主要价值：

- 让用户直观看到仓库、分支、工作区变化和提交历史。
- 支持文件暂存、提交、合并、撤销、重置和远程同步等基础流程。
- 使用 LibGit2Sharp 直接操作仓库，不依赖系统 Git CLI。
- 对部分危险操作创建本地恢复点，并记录等价 Git 命令和结构化操作日志。
- 提供轻量文本编辑器，并把 DOCX、PDF、图片等文件交给 Windows 默认程序打开。

## 3. 技术方案

### 3.1 技术栈

| 层面 | 技术 |
| --- | --- |
| 运行平台 | Windows 10/11 x64 |
| 框架 | .NET 10，WPF |
| 架构模式 | MVVM |
| MVVM 工具 | CommunityToolkit.Mvvm 8.4.2 |
| Git 引擎 | LibGit2Sharp 0.32.0 |
| 代码块暂存 | 包内 libgit2 的 `git_apply` 原生互操作 |
| 编辑器 | AvalonEdit 6.3.1.120 |
| 本地数据库 | Microsoft.Data.Sqlite 10.0.10 |
| 凭据存储 | Windows Credential Manager |
| 测试 | xUnit 2.9.3 |

SDK 版本固定在根目录 `global.json` 中，目前要求 `10.0.302`。

### 3.2 解决方案结构

| 项目 | 职责 |
| --- | --- |
| `src/GitVisualizer.App` | WPF 主界面、ViewModel、提交图、对话框、主题和中文资源 |
| `src/GitVisualizer.Core` | 领域模型和服务接口，不直接依赖 WPF 或 LibGit2Sharp |
| `src/GitVisualizer.Infrastructure` | Git、差异、文件系统、恢复点、SQLite、凭据和诊断日志实现 |
| `tests/GitVisualizer.Tests` | 单元测试和临时仓库集成测试 |

主要代码入口：

- 应用启动和依赖注入：`src/GitVisualizer.App/App.xaml.cs`
- 主窗口布局：`src/GitVisualizer.App/MainWindow.xaml`
- 界面事件：`src/GitVisualizer.App/MainWindow.xaml.cs`
- 主状态与命令：`src/GitVisualizer.App/ViewModels/MainWindowViewModel.cs`
- 核心接口：`src/GitVisualizer.Core/Interfaces.cs`
- Git 实现：`src/GitVisualizer.Infrastructure/Git/LibGitRepositoryService.cs`
- 提交图：`src/GitVisualizer.App/Controls/CommitGraphControl.cs`
- 恢复服务：`src/GitVisualizer.Infrastructure/Recovery/RecoveryService.cs`

## 4. 当前功能状态

交接时应区分“后端已提供能力”和“界面已经可以操作”。部分功能虽然存在于 `IGitRepositoryService`，但主窗口尚无入口。

### 4.1 已在界面中提供的功能

- 仓库：
  - 打开已有仓库。
  - 打开普通文件夹时询问是否初始化，确认后初始化并直接打开。
  - 手动初始化仓库。
  - 从 HTTPS、SSH 或本地路径克隆仓库。
  - 最近仓库切换，当前选择保持在活动仓库。
  - 按创建时间、修改时间或目录大小排序。
- 提交历史：
  - 按当前活动仓库显示提交关系图。
  - 首次加载 200 个提交，可继续分页加载。
  - 查看提交 ID、作者、时间、父提交和引用装饰。
  - 从提交创建分支、cherry-pick、revert 和 soft/mixed/hard reset。
- 工作区：
  - 查看未暂存和已暂存文件。
  - 整文件暂存、取消暂存，以及两个区域之间拖放。
  - 生成统一差异并暂存或取消暂存当前差异块。
  - 创建普通提交和 amend。
- 分支：
  - 显示本地和远程分支。
  - 双击或右键切换本地分支。
  - 合并选中分支。
- 文件：
  - 浏览三层目录树。
  - 新建文件、文件夹和有效的空白 DOCX。
  - 重命名、删除文件或文件夹。
  - 从资源管理器拖入文件，目标冲突时不覆盖。
  - 编辑和保存文本文件，保留编码与换行符。
  - 1 秒防抖自动保存，并在首次启用时提示风险。
  - DOCX、PDF、Office 文件和图片使用系统默认程序打开；文件树双击可直接打开。
- 远程：
  - fetch、pull、push。
  - pull 支持 merge、rebase、ff-only，并按仓库记录选择。
  - HTTPS 用户名/PAT 存储在 Windows Credential Manager。
  - SSH 使用 Windows SSH Agent。
- 冲突：
  - 显示 ours、结果、theirs 三栏。
  - 使用任一侧、保留两侧、手动编辑并标记解决。
  - 继续或中止进行中的 Git 操作。
- 可追溯性：
  - 保存仓库级操作日志。
  - 选择日志后显示该次操作对应的等价 Git 命令。
  - 检测 LFS、子模块和 Hooks 并给出提示。

### 4.2 后端已有但界面未接入的能力

以下接口或实现已存在，但普通用户无法在当前主界面完整使用：

- 分支重命名、安全删除和强制删除。
- 标签创建和删除。
- stash 创建、应用、pop 和删除。
- rebase onto。
- remote 添加、编辑和删除。
- force push/force-with-lease。
- 放弃工作区修改。
- 提交间比较。
- 恢复点列表、查看和一键恢复。

接手者增加界面时应优先复用现有接口，不要在 View 或 code-behind 中直接调用 LibGit2Sharp。

### 4.3 本地数据

运行数据位于 `%LocalAppData%\GitVisualizer`：

| 路径 | 内容 |
| --- | --- |
| `settings.json` | 最近仓库、自动保存、最后仓库和 Pull 策略 |
| `state.db` | SQLite 操作日志 |
| `Recovery` | 恢复点 ZIP 归档 |
| `Logs` | 按天诊断日志，默认保留 14 天 |

当前实现与最初方案略有差异：最近仓库保存在 `settings.json`，并未保存在 SQLite。

## 5. 构建、运行与测试

### 5.1 环境要求

- Windows 10/11 x64。
- .NET SDK 10.0.302 或兼容的 10.0 最新补丁。
- 不要求安装系统 Git。

常规命令：

```powershell
dotnet restore
dotnet build GitVisualizer.slnx -c Release
dotnet test GitVisualizer.slnx -c Release --no-build
dotnet run --project src/GitVisualizer.App
```

当前开发机的 SDK 安装在用户目录。如果 `dotnet` 默认命令找不到 SDK，可执行：

```powershell
& "$env:LOCALAPPDATA\Microsoft\dotnet\dotnet.exe" build GitVisualizer.slnx -c Release
& "$env:LOCALAPPDATA\Microsoft\dotnet\dotnet.exe" test GitVisualizer.slnx -c Release --no-build
& "$env:LOCALAPPDATA\Microsoft\dotnet\dotnet.exe" run --project src/GitVisualizer.App
```

Release 输出位于：

`src\GitVisualizer.App\bin\Release\net10.0-windows`

注意：当前未制作自包含发布包。在本机直接双击 `GitVisualizer.exe` 可能因找不到用户级 .NET 运行时而提示安装 .NET；使用上述 `dotnet run` 或 `dotnet GitVisualizer.dll` 可以运行。正式交付前应增加 self-contained 发布或安装程序。

### 5.2 当前测试基线

交接时 Release 构建结果为 0 个警告、0 个错误，自动化测试为 11/11 通过。

现有测试覆盖：

- 仓库识别与初始化。
- 文件暂存、提交和历史读取。
- 分支切换、合并和 revert。
- 推送到临时 bare 仓库。
- 选定差异块暂存。
- 放弃修改后的恢复。
- 仓库切换与提交图同步。
- 仓库排序和日志对应命令。
- 文本编码、CRLF、外部修改保护和二进制只读。
- DOCX Open XML 包结构。
- 提交图集合更新。

尚缺少正式自动化覆盖的场景见第 6 节。

## 6. 已知不足与风险

### 6.1 高优先级

1. **恢复功能没有完整交付到界面**

   `RecoveryService` 会创建隐藏引用和 ZIP 文件，但 `MainWindowViewModel` 仅注入了服务，没有列出或恢复入口。操作日志中的恢复点 ID 也无法点击恢复。

2. **恢复归档与恢复动作不完全对称**

   `RecoveryService.cs` 会归档 `git-index`，但 `RestoreAsync` 目前只恢复 `files/` 下的工作区文件，没有恢复 index。删除文件在创建恢复点时如果已不存在，也不会进入 ZIP；主要依赖隐藏引用找回已提交内容。

3. **超过 500 MB 时的确认流程未实现**

   当前只实现了 30 天、50 个和总量 2 GB 的清理规则，没有在单次预计超过 500 MB 时暂停并询问用户。

4. **SSH 主机指纹校验未实现**

   当前 SSH 仅通过 `DefaultCredentials` 使用系统 Agent，没有首次主机指纹确认、已知主机记录和指纹变化阻断。`CredentialKind.SshKey` 也未真正使用 `PrivateKeyPath`。

5. **关闭程序前没有未保存内容确认**

   编辑器支持显式保存和自动保存，但主窗口没有 `Closing` 检查。用户关闭程序时，尚未保存的文本可能丢失。

6. **直接双击 EXE 的运行体验不稳定**

   当前是 framework-dependent 构建，且开发机使用用户级 SDK。没有安装包、运行时检测、自包含发布或自动更新。

### 6.2 中优先级

1. **大量 Git 后端功能没有 UI**

   标签、stash、rebase、remote 管理、分支重命名/删除、force push、放弃修改和恢复点均需补充 ViewModel 命令、预览对话框和入口。

2. **代码块暂存只能默认处理第一个差异块**

   `MainWindowViewModel.cs:463` 使用 `hunks.FirstOrDefault()`。右侧差异页只是纯文本，没有差异块列表、勾选状态或逐块导航，因此不是真正完整的交互式 hunk 选择。

3. **脏工作区处理仅报错，没有引导**

   checkout、merge、cherry-pick、rebase 和 pull 会要求工作区干净，但当前只返回“请先提交、暂存工作现场或取消操作”，没有提供“提交 / stash / 取消”三选一流程。

4. **冲突解决器不是完整逐块合并工具**

   Core 和 ViewModel 保存了 base 内容，但 XAML 没有 base 查看入口；当前操作粒度是整文件 ours/theirs/both 或手工编辑，不支持逐冲突块选择。

5. **远程操作默认只使用第一个 remote**

   fetch、push 和凭据设置均取 `Remotes.FirstOrDefault()`，多 remote 仓库无法选择目标。界面也没有 upstream、远程分支和 refspec 管理。

6. **提交图没有真正虚拟化**

   历史按 200 条分页读取，但 `CommitGraphControl` 会对已加载的全部节点重新布局和绘制。多次加载后仍可能出现卡顿，尚未达到 5 万提交仓库的性能验收要求。

7. **文件树不是按需加载**

   根目录最多读取 2000 项，子目录每层最多 500 项，固定递归三层。深层文件不可见，大目录会被截断，也没有“加载更多”提示。

8. **按文件大小排序开销较大**

   选择“文件大小”会递归扫描每个最近仓库的全部文件。虽然放在后台线程执行，但没有进度、缓存或取消入口，大仓库和 OneDrive 目录可能较慢。

9. **外部程序临时文件会出现在文件树和 Git 状态**

   例如 WPS 打开 DOCX 时可能产生 `~$*.docx`。当前文件树只排除 `.git`，不会区分编辑器临时文件。建议提供可配置过滤提示，但不要未经用户同意自动修改 `.gitignore`。

10. **国际化只完成了部分准备**

    已有 `Strings.zh-CN.xaml`，但许多按钮、提示和错误仍直接写在 XAML/C# 中，无法完整切换语言。

### 6.3 测试和质量不足

- 没有覆盖 clone/fetch/pull 的 HTTPS Token 实际服务器测试。
- 没有 SSH Agent、错误凭据和主机指纹变化测试。
- 没有两份克隆制造分叉后验证三种 Pull 策略的端到端测试。
- 没有 merge、rebase、cherry-pick 三类冲突的完整继续/中止测试矩阵。
- 没有 amend、rebase、hard reset、强制删除等操作的完整恢复测试。
- 没有 10 万文件和 5 万提交的性能基准。
- 没有长路径、大小写路径、只读文件、中文路径的系统性测试。
- 没有扫描真实日志与 SQLite，验证 Token、口令和私钥绝不落盘的安全测试。
- 没有完整 WPF UI 自动化测试；现有提交图测试只验证集合变化后的尺寸更新。

## 7. 已知布局和位置问题

下表中的“位置”既表示界面区域，也给出主要代码位置，方便接手者直接修改。

| 区域 | 当前问题 | 代码位置 | 建议 |
| --- | --- | --- | --- |
| 主窗口底部 | 暂存区固定为 250 px，不是可折叠抽屉；文件较少时出现大块空白，窗口较矮时又挤压提交图 | `MainWindow.xaml:42` | 改为 `GridSplitter` + 可折叠抽屉，并保存用户高度 |
| 右侧详情区 | 固定宽度 400 px；在最小窗口宽度下占比过大，中间关系图过窄 | `MainWindow.xaml:89` | 改为比例宽度并允许用户拖动，设置合理的最小/最大宽度 |
| 左侧仓库与分支 | 仓库区固定 130 px、分支区固定 180 px；列表较多时空间分配不合理 | `MainWindow.xaml:96`、`MainWindow.xaml:98` | 左侧内部增加纵向分隔条或折叠分组 |
| 顶部工具栏 | 所有按钮、仓库信息和自动保存位于单行 DockPanel；窗口接近最小宽度时可能拥挤或截断 | `MainWindow.xaml:47-76` | 使用可溢出工具栏、菜单或响应式隐藏低频按钮 |
| 冲突解决器 | ours/result/theirs 被放进右侧约 400 px 的三等分栏，每栏实际太窄；base 内容无显示位置 | `MainWindow.xaml:302-339` | 冲突解决器改为独立全宽页面或弹窗，增加 base 切换页 |
| 提交关系图 | 节点在左侧，但提交文字固定从 150 px 开始，简单线性历史中间存在明显空隙 | `CommitGraphControl.cs:20` | 根据实际 lane 数动态计算文本起始位置 |
| 提交图选中反馈 | 点击提交后只有详情变化，图上的节点没有持续选中高亮 | `CommitGraphControl.cs` | 增加 `SelectedCommit` 依赖属性和选中背景 |
| 文件树 | 每个文件前都显示同一个 `▸` 文本，文件和目录视觉区分不足；深层目录被固定三层截断 | `MainWindow.xaml:27-34`、`MainWindowViewModel.cs:776-791`、`:996-1027` | 使用文件/文件夹图标和真正的惰性展开 |
| 新建文件菜单 | 菜单模拟 Windows 类型，但类型和图标为硬编码，不能读取系统“新建”注册项 | `MainWindow.xaml:151-168` | 后续可增加可配置模板，不建议直接完整枚举注册表 |
| 右侧标签页 | 差异、编辑器、详情、冲突和日志共用窄栏；复杂差异和冲突不适合此位置 | `MainWindow.xaml:213-370` | 支持将差异/冲突切换到中央主区域，右侧只保留摘要 |

## 8. 建议扩展方向

### 8.1 第一阶段：补齐安全闭环

- 增加恢复中心：列出恢复点、显示来源操作、预览内容、恢复到分支或逐文件恢复。
- 补齐 index 恢复、删除文件恢复和 500 MB 预警。
- 增加窗口关闭/仓库切换/文件切换时的未保存检查。
- 为危险操作统一使用 `GitOperationPreview`，不要散落多个不同格式的 MessageBox。
- 补齐 SSH 主机指纹校验和 known-hosts 管理。

### 8.2 第二阶段：补齐已有后端功能的界面

- 分支重命名、安全/强制删除。
- 标签和 stash 管理。
- rebase onto。
- remote 增删改、目标 remote 选择和 upstream 管理。
- force-with-lease 高风险入口。
- 放弃修改和提交间比较。

### 8.3 第三阶段：提升核心交互

- 可勾选的差异块列表和逐块暂存。
- 全宽冲突工作台，支持 base、逐块选择、行级标记和同步滚动。
- 提交图虚拟化、选中状态、分支筛选、搜索和跳转。
- 文件树惰性加载、搜索、过滤和刷新状态提示。
- Pull 分叉图示和按钮选择，替换输入 `merge/rebase/ff-only` 的文本框。

### 8.4 第四阶段：工程化与发布

- 添加 self-contained 或 single-file 发布配置。
- 制作 MSIX/WiX 安装包、应用图标和正式产品名称。
- 增加 UI 自动化、性能基准和安全回归测试。
- 完成深色/浅色手动切换与完整资源国际化。
- 增加无障碍名称、键盘导航和高 DPI 多显示器测试。

### 8.5 长期扩展

- GitHub/GitLab/Gitee 平台登录、PR、Issue 和代码评审。
- LFS 和子模块管理。
- 多仓库工作区、工作树（worktree）和子仓库导航。
- 可视化交互式 rebase，包括 reorder、squash、fixup、drop 和 edit。
- 后续另建 Git 服务端和多用户协作系统；不建议与当前桌面客户端代码混在同一项目中。

## 9. 推荐接手顺序

1. 先运行 Release 构建和 11 个测试，确认环境一致。
2. 阅读 `Interfaces.cs`、`MainWindowViewModel.cs` 和 `LibGitRepositoryService.cs`，理解 UI 与 Git 层边界。
3. 优先完成恢复中心、未保存检查和 SSH 指纹校验。
4. 把已有但未接入的 Git 功能逐项接到界面，并为每项增加危险操作预览。
5. 重构主窗口布局，将底部固定区改为可调整抽屉，将冲突解决器移到更宽区域。
6. 完成代码块选择和提交图虚拟化后，再进行大仓库性能验收。
7. 最后处理安装包、品牌、国际化和平台服务集成。

## 10. 交接结论

当前代码已经验证了“无需 Git CLI 的 WPF Git 客户端”这一技术路线，仓库初始化、提交图、文件编辑、暂存提交、基础分支操作、远程同步和冲突处理可以形成基本演示闭环。

但当前版本仍属于功能原型：部分 Git 能力只完成后端，没有可见入口；恢复、安全认证、交互式差异、性能和响应式布局尚未达到最初 V1 方案的完整验收标准。后续开发应优先补齐安全闭环和真实可操作性，再扩展平台能力或制作安装包。
